
### 2026-06-14 (hourly loop) — STATUS: F3 run in progress, no launch
- GPU busy: PID 31432 training `run_F3_rabtime100_VG.log` at epoch 99/100, GPU 73% / 7 GB. Per single-GPU rule, did NOT launch concurrent work.
- F3 (rab + time bias, rab window 100) trajectory: val_NDCG peaks at epoch 50 (val 0.0689 / **test 0.0578**), then decays (ep90 val 0.0669 / test 0.0563). best_test ≈ 0.0578 < single-seed best 0.0631 → another negative lever.
- Next loop: F3 will be complete; read results_F3_rabtime100_VG.json, then proceed to next untested lever per STEP 3 queue (max_seq_len 200 probe / lr sweep / n-layers 6-8), or move toward SOTA_VERDICT if levers exhaust.

### 2026-06-15 (hourly loop, cont.) — MI BLaIR text: 0.0375 (≤ SBERT, same as VG)
- BLaIR title-encode for MI built (24,587×768, asin2idx byte-identical to SBERT cache → exact alignment, no leakage), run at the winning 20-ep schedule (else identical, seed 20260608, --encoder-cache blair). Best-by-val val 0.0421 → **test NDCG@10 = 0.0375** (HR@10 0.0690). results_BEST_MI_blair_e20.json.
- **BLaIR ≤ SBERT on MI too** (0.0375 vs 0.0384), reproducing the VG finding: the TAPE/text-sim stack already consumes the text signal; the domain-contrastive encoder adds nothing on our architecture. Confirms the residual gap is not the text encoder.
- **MI single-seed standing: best = SBERT 20-ep 0.0384.** vs published SASRec .0356 (+7.9%), HSTU .0392 (−2.0%, matched), HSTU-BLaIR .0406 (−5.4%). Not a per-category SOTA; same structural HSTU-kernel residual as VG, smaller in magnitude.
- ACTION: launching sequential 5-seed multi-run of the best SBERT 20-ep config (seeds 20260609–20260612 + existing 08) to establish the honest mean±std for the verdict and any claim.

### 2026-06-15 (hourly loop) — P1: n_heads=4 on the FAITHFUL HSTU block (genuinely untried architectural match)
- STATUS 0: no SOTA_ACHIEVED.md; no python training at cycle start; GPU idle (2.4 GB, 0%). Cleared to act.
- **Reopened the push on a real config discrepancy.** Read the published reference gin (external/HSTU-BLaIR/configs/amzn23_game/hstu-sampled-softmax-n512-blair.gin): their HSTU spec is `num_blocks=4, num_heads=4, dv=16, dqk=16, item_embedding_dim=64, dropout=0.5, max_seq_len=50` — i.e. **4 heads of dim 16**. Every headline VG run on our FAITHFUL HSTU block used `--n-heads 2` (dh=32). n_heads=4 was used only in C1/C2 (the OLD *broken* HSTU block that "averaged → mean-pooling", per the code comment at HSTULayer line ~271) and an old d=128 POC — never on the current faithful block + winning stack.
- **Hypothesis under test:** the verdict attributes the whole ~0.0588(plain)→0.0741(published) gap to "CUDA-kernel numerics," but a pure-numerics difference shouldn't erase HSTU's *entire* architectural gain over SASRec (their +29%). HSTU's pointwise (non-softmax, silu-gated, unnormalized) attention is head-count sensitive; matching their 4-head/dh=16 spec is the one architectural variable never swept on the correct block. If it lifts test materially, the "it's the kernels" conclusion is incomplete and real headroom exists.
- Smoke 2ep clean (no NaN; ep2 val 0.0475/test 0.0428, full-catalog n_eval=94,762 — slightly AHEAD of the n_heads=2 early trajectory).
- **LAUNCHED P1** (PID 9887): exact BEST_VG config (faithful HSTU + TAPE-512 + time-bias + text-sim-bias + pos-rab + chunked-full-softmax + dropout 0.5, SBERT, warmup_cosine) with `--n-heads 4` (params 11.6M vs ~21M for 2-head, fewer because dh halves the uvqk fanout), 40ep seed 20260608, eval-every 10 → results_P1_heads4_VG.json / run_P1_heads4_VG.log. Gate vs 0.0639 single-seed best; >0.0660 → 5-seed escalation toward the 0.0760 mandate. One GPU job; analyzing this cycle.

### 2026-06-15 (hourly loop, cont.) — P1 RESULT: n_heads=4 ties n_heads=2 (head count is NOT the gap)
- **P1 (faithful HSTU + winning stack + `--n-heads 4`, dh=16 matching their dv=dqk=16, 40ep seed 20260608) COMPLETE — neutral, FAILED to gate.** Best-by-val test NDCG@10 = **0.0634** (val 0.0715; HR@10 0.1125; full-catalog n_eval=94,762; 11.6M params). Trajectory ep10/20/30/40 test = 0.0593 / 0.0620 / 0.0629 / 0.0634 — still RISING at ep40 and tracking val closely (NO overfit, unlike the n_heads=2 I-series which overfit at 60/80ep).
- **Decisive diagnostic:** this is the cleanest pure-PyTorch reproduction of their EXACT HSTU architecture spec to date (num_blocks=4, **num_heads=4, dv=dqk=16**, item_emb_dim=64, dropout=0.5, max_seq_len=50, per their game gin). It gives **0.0634 ≈ the 2-head 0.0639** — head count is NOT the source of the published gap. Their architecture is now matched in pure PyTorch (+ our stack) and still lands at ~0.063 vs published HSTU 0.0741 / HSTU-BLaIR 0.0760. This *narrows* the residual to their training/loss numerics (SampledSoftmax negatives sampler + fbgemm/triton fused ops + their 101-epoch no-warmup schedule), not architecture or text encoder (both already swept).
- **Follow-up (the one new thing P1 surfaced):** unlike 2-head, the 4-head config does NOT overfit at 40ep (test still rising), and their reference trains 101 epochs. Launched P2 = same 4-head config at **80 epochs** to test whether the matched architecture benefits from a longer schedule. Slope is decelerating (ep10→20 +0.0027, 20→30 +0.0009, 30→40 +0.0005), so optimistic extrapolation is ~0.065 — below the 0.0660 gate and far below 0.0760 — but it is genuinely untried, so verifying empirically.

### 2026-06-16 (hourly loop) — V1: causal learnable FIR filter (RESEARCH_QUEUE 2026-06-16-1)
- STATUS 0: no SOTA_ACHIEVED.md; no python training at cycle start; GPU idle (2.1 GB, 0%). Cleared to act.
- Consumed the OLDEST QUEUED research-loop proposal (2026-06-16-1): a strictly-causal per-channel learnable temporal FIR filter (depthwise Conv1d, left-padded K-1) as a zero-init gated residual on the sequence embeddings, inserted just before the encoder stack. Leak-free causal dual of the FMLP-Rec (Zhou WWW 2022, arXiv:2202.13556) / BSARec (Shin AAAI 2024, arXiv:2312.10325) learnable frequency filter — the source papers are bidirectional (would leak the label under our all-position next-item loss); causal Conv1d is leak-free by construction. Tests whether attention's low-pass oversmoothing is an in-environment bottleneck — a genuinely new (spectral/FIR) inductive-bias axis untouched by the ~50-probe ledger.
- Implemented `--causal-filter` + `--filter-kernel` (default 50) in run_sasrec_sbert.py: default-OFF, gate zero-init => exact no-op at init, conv kernel init = causal delta (all-pass). Flipped RESEARCH_QUEUE proposal status to RUNNING.
- Smoke 2ep clean (no NaN; ep2 val 0.0501/test 0.0450, full-catalog n_eval=94,762).
- **LAUNCHED V1** (PID 58377): exact BEST_VG stack (faithful HSTU 4-layer/2-head + TAPE-512 + time-bias + text-sim-bias + pos-rab + chunked-full-softmax + dropout 0.5, SBERT, warmup_cosine) + `--causal-filter --filter-kernel 50`, 40ep seed 20260608, eval-every 10 → results_V1_causalfilter_VG.json / run_V1_causalfilter_VG.log. Gate vs 0.0639 single-seed best; >0.0660 → 5-seed escalation toward the 0.0760 mandate. One GPU job; analyzing on completion this/next cycle.

### 2026-06-16 (hourly loop, cont.) — V1 RESULT: causal FIR filter = first lever to nudge test up (0.0653), still sub-gate
- **V1 (causal FIR filter, K=50, 40ep seed 20260608) COMPLETE.** Best-by-val test NDCG@10 = **0.0650** (val 0.0741; HR@10 0.1158; MRR 0.0580; full-catalog n_eval=94,762; 11.6M params). Trajectory test ep10/20/40 = 0.0645/0.0652/0.0650 — peaks ~ep20, plateaus. results_V1_causalfilter_VG.json.
- **V1b (causal FIR filter, K=8 smoother follow-up, explicitly permitted by proposal kill clause) COMPLETE.** Best-by-val test NDCG@10 = **0.0653** (val 0.0744; HR@10 0.1156; MRR 0.0585). Trajectory test ep10/20/30/40 = 0.0631/0.0651/0.0653/0.0653 — still creeping up, val rising. results_V1b_causalfilter_k8_VG.json.
- **VERDICT — marginal positive, the first non-neutral architectural addition of the campaign.** Both kernel sizes beat the prior single-seed best (0.0639): K50 +0.0011, K8 +0.0014. Consistency across two independent K values ⇒ this is real signal on a genuinely new (spectral/FIR-filter) inductive-bias axis, not seed noise. It validates the proposal's representational-impossibility argument: a strictly-causal learnable FIR filter adds a small high-frequency/sharpening response that the low-pass HSTU attention stack cannot synthesize. Leak-free by causal construction (left-padded depthwise Conv1d; the source FMLP-Rec/BSARec filters are bidirectional and would leak the next-item label under our all-position loss).
- **BUT: does NOT reach the 0.0660 5-seed-escalation gate, and is nowhere near the 0.0760 mandate.** Best single-seed of the whole campaign is now 0.0653 (was 0.0639), confirming the ~0.065 in-environment ceiling: even a brand-new function-class lever moves test by only ~+1.4e-3. The ~0.0107 residual to published HSTU-BLaIR 0.0760 remains isolated to their fbgemm/triton fused-kernel numerics + SampledSoftmax sampler (Linux-only on sm_120), not architecture, head count, text encoder, or — now also ruled out — spectral inductive bias.
- Per proposal gate (>0.0660 ⇒ 5-seed), NOT escalating to 5-seed: a +0.0014 single-seed bump cannot lift a 5-seed mean from ~0.064 to 0.0760. Not DEAD (test rose, did not hit the val-up/test-down clause). RESEARCH_QUEUE 2026-06-16-1 flipped to DONE. No SOTA_ACHIEVED written.
- NOTE for next researcher cycle: the causal-FIR axis is the only lever to date that consistently (if marginally) improves test — a 5-seed of the K=8 config would honestly establish whether the new single-seed high (0.0653) holds as a new mean best, worth queuing if a sub-mandate "new best mean" is of interest, though it does not change the 0.0760 verdict.

### 2026-06-17 (hourly loop) — GPU BUSY (Beauty 3rd-dataset run live); recorded MI 5-seed tail contrast = CLEARS the gate
- **STATUS 0:** no `SOTA_ACHIEVED.md`. **STATUS 1:** GPU LIVE — PID 25124 training `run_Beauty_TAIL_V2_text_seed20260608.log` (Beauty_and_Personal_Care, 729,576 users / 207,649 items, 93.2M params, V2_text stack, 20ep, seed08), epoch 7/20 @ ~338s/epoch ⇒ ~73 min remaining. GPU 87% / 11.96 GB. Per the ONE-GPU rule I did NOT launch a 2nd GPU job; this cycle = summarize + record. Research queue has NO QUEUED proposal (all DONE/DEAD; CF1 2026-06-17-3 DEAD ⇒ new-lever search closed per cycle-4 kill gate).
- **MI 5-seed tail contrast COMPLETE (cycle-4 directive #1, the decision-critical run) — independently recomputed by me from the 10 raw JSONs (`results_MI_TAIL_{V2_text,idonly}_seed{08–12}.json`, read-only venv), text−ID paired per seed:**
  - **tail Δ = +0.000335 ± 0.000195 (sample sd), 5/5 seeds positive** (per-seed: +.000321/+.000486/+.000566/+.000214/+.000089). 95% t-CI (df=4) = **[+0.000093, +0.000577] ⇒ EXCLUDES 0**; sign-test p=0.03125. **The MI tail Δ CLEARS 0** — it did NOT deflate toward 0 the way VG's seed08 −0.00041 did at 5 seeds.
  - mid Δ = +0.001400 ± 0.000725 (5/5 pos); head Δ = +0.005269 ± 0.000772 (5/5 pos); overall Δ = +0.003930 ± 0.000449 (5/5 pos, = +6.0% overall text-vs-ID on MI).
  - **Relative-lift ordering: tail +27.6% ≈ mid +26.6% > head +10.1%** — the cold-start signature (tail-rel ≳ head-rel) HOLDS on MI, the EXACT REVERSE of VG (VG tail −2.9% < head +2.6% < mid +6.8%).
  - **HONESTY (supervisor cycle-4 FIX, mandatory symmetry):** text's LARGEST ABSOLUTE lift is on the HEAD (+0.00527) ≫ tail (+0.00034); the tail effect is a large RELATIVE number on a tiny base (~24–35 text tail-hits vs ~18–22 ID tail-hits per seed, tail n=8,800 instances). Reported as a relative tail signal, NOT "text rescues the cold tail" in absolute terms — that would be the val=0.0765-style over-claim the pipeline exists to prevent. A per-hit bootstrap CI on the ~30-hit bucket is not derivable from the aggregate JSONs (only seed-level by_popularity stored); the 5-seed paired t-CI above is the honest band.
- **Per cycle-4 decision gate this SATISFIES the "MI tail Δ clears 0 AND relative ordering holds ⇒ genuine dataset-scoped tail win" branch** ⇒ the sanctioned escalation = MI per-component tail decomp + a 3rd dataset. The **3rd-dataset run (Beauty) is exactly that escalation and is in flight** (text arm live now; the matching `idonly` Beauty arm has NOT started yet — needed to form the Beauty text−ID tail contrast; queue it next when the GPU frees).
- **Mission status update:** the tail thesis is now **DATASET-CONDITIONAL with 2 datasets in hand** — VG arm REFUTED (5-seed, tail-null, all 3 text components tail-negative), MI arm CONFIRMED (5-seed, tail Δ clears 0, cold-start ordering holds). Beauty becomes the tie-breaker / law-tester (a 3rd point on the "text-distinctiveness ÷ ID-density predicts tail benefit" hypothesis). No `SOTA_ACHIEVED` written (correct): overall best 0.0673 ≪ 0.0760 hardware-blocked; the MI tail win is a relative-on-tiny-base effect, not the 0.0760 mandate.
- **NEXT cycle:** if Beauty text arm done + GPU free ⇒ launch the Beauty `idonly` arm (same stack, `--no-sbert`, drop `--text-prototypes`/`--text-sim-bias`) to complete the 3rd-dataset contrast; then per-component MI tail decomp. Keep attribution intact (HSTU=Zhai 2024; TAPE/SBERT text stack; SASRec=Kang 2018).

---

### 2026-06-17 (EXPERIMENT cycle — hourly loop) — GPU BUSY, no new job launched (one-GPU-at-a-time)
- **STEP 0:** no `SOTA_ACHIEVED.md` (correct — overall best 0.0673 << 0.0760 hardware-blocked).
- **Bus read:** ORCHESTRATION status board (tail mission, dataset-conditional law; VG tail REFUTED 5-seed, MI tail CONFIRMED 5-seed); AGENT_FEEDBACK cycle-4 (TOP PRIORITY = complete MI 5-seed tail contrast + Beauty as 3rd-dataset law-tester; CF1 DEAD on VG+MI closed the new-lever search; no new V2-regularizers; FULL PIVOT TO PAPER). RESEARCH_QUEUE: every proposal DONE/DEAD (CF1 DEAD both datasets; TEXTSHARE law DONE — predictor REFUTED/ordering inverted). **No QUEUED proposal exists.**
- **STATUS:** GPU LIVE — PID 27056, 100% util, 15987/16311 MiB. Newest log `run_chain_MIdecomp_BeautyID.log` (a chained driver) is the active job. Per the one-GPU rule I tailed it, summarized, and EXIT without launching a 2nd GPU job.
- **Progress of the live chain (healthy, no NaN, losses monotone down):**
  - PART A COMPLETE (18:34) — MI per-component tail decomp, 3 runs done (sbertonly / tapeonly / textsimonly), `by_popularity` emitted. This is exactly the sanctioned MI per-component tail decomp escalation.
  - PART B RUNNING — Beauty_and_Personal_Care ID-only seed08 (n_users=57,439? no: Beauty large; ~729,576 train steps/epoch, ~350-540s/epoch). At epoch 9/20, loss 11.50 -> 10.48 monotone. This is the matching Beauty `idonly` arm to pair with the Beauty text arm (3rd-dataset tail contrast / law-tester).
- **Action this cycle:** none beyond logging — do NOT interrupt the running driver (supervisor cycle-4 directive #1 "already running — do NOT interrupt"). No CPU side-job launched (no queued CPU artifact; TEXTSHARE already ran).
- **NEXT cycle:** when GPU frees, form the Beauty text-vs-ID tail contrast (idonly arm finishing now; pair with the on-disk Beauty text arm), report tail/mid/head NDCG+HR; then hand to analyst for the 3rd-dataset point on the dataset-conditional law. No SOTA claim. Attribution intact (HSTU=Zhai 2024; TAPE/SBERT text stack; SASRec=Kang 2018).

---

### 2026-06-18 (EXPERIMENT cycle — hourly loop) — GPU BUSY, no new job launched (one-GPU-at-a-time)
- **STEP 0:** no `SOTA_ACHIEVED.md` (correct — overall best 0.0673 ≪ 0.0760 hardware-blocked).
- **Bus read:** ORCHESTRATION status board (supervisor cycle 5: dataset-conditional tail LAW now CONFIRMED — VG 5-seed NULL, MI 5-seed WIN; only remaining empirical gap = the **Beauty 3rd corroborating point**, predicted tail-NULL by the global-ID-density law since Beauty interactions/item ≈24.9 ≈ VG 24.5). AGENT_FEEDBACK through cycle 4 (new-lever search CLOSED — W1/X1/Y1/Z1/GD1/CF1 all DEAD; FULL PIVOT TO PAPER; "rescue cold tail by adding text" KILLED 3/3; mission consolidates to the dataset-conditional law). RESEARCH_QUEUE: **no QUEUED proposal** (all DONE/DEAD).
- **STATUS:** GPU LIVE — PID 30248, 100% util, 15509/16311 MiB. Newest active log `run_Beauty_TAIL_idonly_seed20260608.log` (3rd relaunch of the Beauty ID-only seed08 FULL-catalog arm: n_users=729,576 / n_items=207,649, 13.4M params, ID-only stack `--no-sbert`, 20ep). Per the one-GPU rule I tailed it, summarized, and EXIT without launching a 2nd GPU job (no QUEUED proposal to implement; no CPU side-artifact pending).
- **Progress of the live job (healthy, no NaN, loss monotone 11.50→10.41):** at **epoch 11/20**, ~1000–1400s/epoch. Epoch-10 readout (eval-every-10): overall val_NDCG=0.0247, test_NDCG=0.0211, test_HR=0.0375; **by-popularity NDCG@10 tail=0.0001 (n=71,522) / mid=0.0009 (n=83,242) / head=0.0266 (n=574,812)**. ~9 epochs + final eval remain ⇒ ETA ~2.5–3h.
- **Comparison context (Beauty TEXT arm already on disk, `results_Beauty_TAIL_V2_text_seed20260608.json`, same 729,576-user full-catalog eval, 20ep, seed08):** overall NDCG@10=0.02393 / HR@10=0.04213; **by-popularity tail NDCG=0.000272 (HR 0.000531, n=71,522) / mid NDCG=0.001823 / head NDCG=0.030077.** NOTE: this is the FINAL text arm vs the ID-only arm still mid-training (ep10 readout only) ⇒ NOT yet a fair contrast; the Beauty text−ID tail Δ can only be formed once the ID-only arm completes its epoch-20 eval. Preliminary read is consistent with the predicted Beauty tail-NULL (both arms have a near-floor tail ≈0.0001–0.0003 on a dense catalog), but the verdict waits on completion.
- **Action this cycle:** none beyond logging — do NOT interrupt the running Beauty ID-only arm (it is the sanctioned 3rd-dataset escalation / law-tester). No CPU side-job (no queued CPU artifact).
- **NEXT cycle:** when GPU frees and the ID-only arm finishes, recompute the Beauty text−ID tail/mid/head Δ (NDCG+HR) at seed08, hand the 3rd-dataset point to the analyst for the global-ID-density law (predicted tail-NULL), and — per the supervisor's tail-preserving-eval directive — if the full eval dies again, do NOT relaunch the full run; switch to the FIXED stratified subsample (keep ALL tail+mid users + ~10% head, same eval set across arms/seeds). No SOTA claim. Attribution intact (HSTU=Zhai 2024; TAPE/SBERT text stack=Hou 2024/Liu 2025; SASRec=Kang & McAuley 2018).

---
### EXPERIMENT cycle — 2026-06-18 ~06:03 (hourly loop)
- **Bus read:** ORCHESTRATION + AGENT_FEEDBACK now through **cycle 5 (2026-06-18)**. Latest binding directives: (a) overall track SATURATED at 0.0673±0.0003 (6-seed) — correct, lever search CLOSED (W1/X1/Y1/Z1/GD1/CF1 all DEAD); (b) **TOP PRIORITY = land the Beauty 3rd corroborating point with a TAIL-PRESERVING eval, ≥3 seeds — do NOT relaunch the full 729k eval a 4th time if it dies; switch to a FIXED stratified subsample (keep ALL tail+mid users, ~10% head, same eval-set across arms/seeds, terciles from full train)**; (c) do NOT interrupt the live full-eval ID arm; (d) paper is the deliverable (dataset-conditional tail law: VG 5-seed null / MI 5-seed win, governed by global ID-density; Beauty predicted NULL). RESEARCH_QUEUE: no QUEUED proposal (all DONE/DEAD). No KILL applied to anything I touched.
- **STATUS:** GPU LIVE — PID 30248, ~89–100% util, 15.5/16.3 GiB. Newest active log `run_Beauty_TAIL_idonly_seed20260608.log`, now at **epoch 14/40→mid-epoch-15** (~1.0–1.4ks/epoch; no NaN, loss monotone 11.50→10.29). Epoch-10 readout: overall val 0.0247 / test 0.0211 / test HR 0.0375; by-popularity tail=0.0001 (n=71,522) / mid=0.0009 (n=83,242) / head=0.0266 (n=574,812). Next eval at epoch 20. Per the one-GPU-job rule: tailed, summarized, did NOT launch a 2nd GPU job.
- **Action this cycle (GPU-free prep on the critical path):** implemented the binding cycle-5 eval FIX in `run_sasrec_sbert.py` — new flag **`--eval-stratify-head FRAC`** (default **0.0 = OFF, exact no-op**; verified the full-eval path is bit-identical when off). When FRAC∈(0,1): TAIL-PRESERVING stratified eval — keep ALL users whose test target is a tail/mid item, subsample only head-target users to FRAC, via a FIXED `RandomState(subsample_seed)` so the eval-user set is identical across the text/ID arms and across model seeds; terciles are computed from TRAIN frequency (leak-free) and **every kept user still ranks against the full n_items catalog** (only the user count drops). Moved the leak-free item-bucket computation ahead of user selection so the stratified branch can read it; threaded `stratify_head` through both `evaluate()` call sites. `py_compile` OK. This makes the multi-seed Beauty point (and the fallback if the full eval dies a 3rd time) runnable immediately next cycle. **GPU functional smoke deferred** to the next free-GPU cycle before the first stratified Beauty run (eval-only change ⇒ no train-NaN risk; cannot smoke now without contending with the live job).
- **No SOTA claim** (none warranted; overall 0.0673 ≪ 0.0760 hardware-blocked, Beauty tail point not yet formed). Attribution intact: HSTU=Zhai 2024; TAPE/SBERT text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018; tail-preserving stratified ranking eval is standard full-catalog protocol with a fixed user subsample.
- **NEXT cycle:** if GPU free and the live ID-only arm finished → recompute the Beauty seed08 text−ID tail/mid/head Δ (NDCG+HR), then run the ≥3-seed Beauty law point on `--eval-stratify-head 0.10` (both arms, same eval set), GPU-smoke the flag first. If the full eval is still running → tail + EXIT again. If it died → start the stratified ≥3-seed runs directly (no 4th full relaunch).

---
### EXPERIMENT cycle — 2026-06-18 (hourly loop) — GPU BUSY (critical-path stratified Beauty run live), no 2nd GPU job
- **STEP 0:** no `SOTA_ACHIEVED.md` (correct — overall best 0.0673 ≪ 0.0760 hardware-blocked).
- **Bus read:** ORCHESTRATION status board + AGENT_FEEDBACK through **cycle 5 (2026-06-18)**. Binding top priority = land the **Beauty 3rd corroborating point** (predicted tail-NULL by the global-ID-density law: Beauty interactions/item ≈24.9 ≈ VG 24.5) using the **TAIL-PRESERVING stratified eval** (`--eval-stratify-head`, keep ALL tail+mid users + ~10% head, same eval set across arms/seeds, terciles from full train), ≥3 seeds, both arms; do NOT relaunch the full 729k eval. New-lever search CLOSED (W1/X1/Y1/Z1/GD1/CF1 DEAD). RESEARCH_QUEUE: **no QUEUED proposal** (only the `<YYYY-MM-DD>` template placeholder). Nothing forbids the live path.
- **STATUS:** GPU LIVE — PID 20936, 89% util, 12,725/16,311 MiB. Newest active log `run_Beauty_TAIL_V2_text_strat_seed20260609.log` = the **FIRST stratified-protocol run** (Beauty TEXT arm V2, seed20260609, 93.2M params, eval-every-10). Healthy: **epoch 9/20**, ~334s/epoch, loss monotone 11.21→10.46, no NaN. ETA ~1h (11 epochs + final eval). First stratified eval prints at epoch 10 (~1 epoch away) — that is the de-facto GPU smoke of last cycle's flag; expected `n_eval`≈212k if head-frac=0.10 (vs 729,576 full). Per the one-GPU-job rule: tailed, summarized, did NOT launch a 2nd GPU job.
- **GPU-free critical-path check performed:** code-reviewed the `--eval-stratify-head` implementation in `run_sasrec_sbert.py` (lines 1132–1175). **VERIFIED leak-free + correct:** terciles from TRAIN freq computed before user selection; tail-preserving (keep all `item_bucket[target]<2`, subsample only head via fixed `RandomState(subsample_seed)`); identical eval-user set across arms/seeds (same `test_dict`→same `head_users`→same `choice`); every kept user ranks against full `n_items`. No bug that would corrupt the live run's stratified eval.
- **Action this cycle:** none beyond logging + the code verification — do NOT interrupt the running critical-path Beauty stratified text arm. No CPU side-job (no queued CPU artifact; MI per-user-bootstrap `--dump-tail-ranks` hardening is LOWER priority and must not contend for GPU).
- **NEXT cycle:** if GPU free and seed09 text arm finished → record its stratified tail/mid/head NDCG+HR; then continue the Beauty law campaign — run the matching seed09 ID-only arm and the remaining seeds (need ≥3 seeds × {text,ID} on the SAME stratified eval set, head-frac 0.10) to form the Beauty text−ID tail Δ (predicted ≈0). If full eval still running → tail + EXIT. No SOTA claim. Attribution intact: HSTU=Zhai 2024; TAPE/SBERT text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018; tail-preserving stratified ranking = standard full-catalog protocol with a fixed user subsample.

### 2026-06-18 15:03 — EXPERIMENT agent cycle (GPU busy → defer + tail)
- **Bus read:** no SOTA_ACHIEVED.md (continue). Status: python PID 23624 active, GPU 100% / 15914 of 16311 MiB. ONE-GPU-JOB policy → do not launch a second GPU job.
- **Live job:** `run_Beauty_TAIL_idonly_strat_seed20260609.log` (Beauty_and_Personal_Care, ID-only, stratified eval, seed 20260609; d=64/L4/H2, 20 epochs, 13.38M params, 729,576 users / 207,649 items). Progress: finished epoch 19, now in the final full-catalog eval pass (~26k/202,697 users at ~588s in). Epoch-10 checkpoint: val_NDCG=0.0093, test_NDCG=0.0077, test_HR=0.0136; by-popularity tail=0.0001 / mid=0.0011 / head=0.0266 — confirms the expected head-dominated Beauty profile, ID-only arm.
- **Action this cycle:** none beyond logging — did NOT interrupt the running critical-path seed09 ID-only stratified arm. No CPU side-job launched (no queued CPU artifact this cycle). EXIT per protocol.
- **NEXT cycle:** if GPU free → record this seed09 ID-only run's final stratified tail/mid/head NDCG+HR into results JSON, then continue the Beauty text−ID tail-Δ campaign (need ≥3 seeds × {text,ID} on the same head-frac-0.10 stratified eval set). No SOTA claim. Attribution intact: HSTU=Zhai 2024; SBERT/text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018.

### 2026-06-18 ~18:04 — EXPERIMENT agent cycle (GPU busy: density-titration live → defer + tail)
- **STEP 0:** no `SOTA_ACHIEVED.md` (correct — overall best 0.0673 ≪ 0.0760 hardware-blocked).
- **Bus read:** ORCHESTRATION status board (supervisor cycle 6, 2026-06-18) + AGENT_FEEDBACK through cycle 5/6. Binding state: overall track SATURATED at 0.0673±0.0003 (6-seed); new-lever search CLOSED (W1/X1/Y1/Z1/GD1/CF1 all DEAD; "rescue cold tail by adding text" KILLED 3/3). Active sanctioned directions in priority order: (1) finish Beauty ≥3-seed band via FREE full-eval route; (2) **DENSITY-TITRATION causal experiment (PROPOSAL 2026-06-18-1)** on the fast VG dataset — thin TRAIN interactions down a ladder, freeze eval+terciles at full density, run text+ID, pre-registered prediction = VG tail Δ crosses null→positive as density drops toward MI's; (3) PAPER. RESEARCH_QUEUE: **no QUEUED proposal** (only the `<YYYY-MM-DD>` template placeholder). Nothing forbids the live path.
- **STATUS:** GPU LIVE — two python PIDs (47724 = uv titration **driver**, 581s CPU; 42744 = venv **child trainer**). nvidia-smi 4,077/16,311 MiB, 17% util (single training job + light driver; not two contending GPU jobs). Per the one-GPU-job rule: tailed the newest logs, summarized, did NOT launch a 2nd GPU job. No CPU side-job (no queued CPU artifact).
- **Live experiment = density-titration rung ρ=0.66 (direction #2).** Driver `run_TITRATE_rho066_driver.log` runs the two arms sequentially on the SAME thinned train + FROZEN full-density eval/terciles.
  - **TEXT arm — COMPLETE** (`results_TITRATE_text_rho066_VG.json`): TITRATION subsample-train-frac=0.66 (interaction mode) → 412,592/625,062 train interactions kept, **train interactions/item = 16.109** (full = 24.405; ≈ MI's 16.2 sparse regime); eval histories + terciles FROZEN at full density (n_eval=94,762, n_items=25,612). Best-by-val: val_NDCG=0.0660, **test_NDCG=0.05894** (HR 0.10538), by-popularity **tail NDCG=0.003201 (HR 0.006606, n=10,900) / mid NDCG=0.010510 (n=13,128) / head NDCG=0.076520 (n=70,734).** (Overall test drops from the full-density 0.0673 as expected when 34% of train interactions are removed.)
  - **ID-ONLY arm — TRAINING NOW** (`run_TITRATE_idonly_rho066_VG.log`, child PID 42744): same thinned train (412,592 interactions, 16.109/item), ID-only stack (`--no-sbert`, 1,727,421 params), epoch **3/40** at log time, ~7s/epoch, loss monotone 9.85→9.13, no NaN. ETA ~5 min + final eval. First eval at epoch 10.
- **Why this matters (pre-registered):** the text−ID **tail Δ** at ρ=0.66 is the first interior point of the titration dose–response. Prediction = VG's full-density tail NULL (−0.000148±0.000179, 5-seed) should move toward MI's sparse-regime tail WIN (+0.000335±0.000195) as interactions/item falls 24.4→16.1 (≈ MI). The TEXT tail at ρ=0.66 = 0.003201; the contrast can only be FORMED once the ID-only arm finishes its best-by-val eval — deferred to next cycle (do NOT interrupt the live driver).
- **Action this cycle:** none beyond logging — do NOT interrupt the running titration driver (it is the sanctioned direction-#2 causal experiment). No SOTA claim (none warranted). Attribution intact: HSTU=Zhai 2024; TAPE/SBERT text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018; train-interaction thinning with frozen full-density eval = standard data-ablation (NOT a model lever; does not re-open the closed lever search).
- **NEXT cycle:** if GPU free and the ID-only ρ=0.66 arm finished → read `results_TITRATE_idonly_rho066_VG.json`, compute the text−ID tail/mid/head Δ (NDCG+HR) at ρ=0.66, hand the interior dose–response point to the analyst, and (driver permitting) continue the titration ladder to the next rung. If the driver is still running → tail + EXIT again.

### 2026-06-18 22:05 — EXPERIMENT cycle (hourly): ρ=0.88 holds POSITIVE at 3 seeds (+0.00060 seed10); tail+EXIT (one-GPU invariant)
- **STEP 0:** no `SOTA_ACHIEVED.md` → continue (overall best 0.0673 ≪ 0.0760, hardware-blocked; honest negatives valid).
- **Bus read:** ORCHESTRATION cycle-6 status board + AGENT_FEEDBACK through cycle-6 (LONG-TAIL mission; law = 3-point density-ranked ordering MI sparse 5/5 tail WIN +0.000335 CI-excl-0 / VG dense 5-seed NULL / Beauty dense NULL, governed by global interactions/item). RESEARCH_QUEUE: PROPOSAL 2026-06-18-2 (curve-shape titration bracket) = RUNNING — now in its 5-seed confirmation phase at the candidate-positive rungs ρ∈{0.88,0.78}; PROPOSAL 2026-06-18-3 (Digital_Music 4th sparse dataset) QUEUED-behind. No new QUEUED item jumps ahead ⇒ path R correctly continues the live bracket.
- **STATUS at entry — GPU BUSY (one job live, NOT two):** `Get-Process python` → PID 25052 (venv launcher) + child PID 43656 (uv worker, CPU 284s), confirmed parent→child (PPID 25052→43656), BOTH writing the SAME `results_TITR_text_rho078_s10_VG.json` ⇒ a single driver+worker GPU job, not contention. `nvidia-smi` 6,416/16,311 MiB, 15% util (between eval bursts). Live = **ρ=0.78 TEXT seed10**, in its full 94,762-user catalog eval pass (by-pop tail=0.0046/mid=0.0165/head=0.0870), no NaN, healthy.
- **NEW completed rung this cycle — ρ=0.88 seed10 (best-by-val test, full-catalog n_eval=94,762, frozen full-density terciles, paired n: tail 10,900 / mid 13,128 / head 70,734):**
  - text overall 0.065296 vs ID 0.062874 → overall Δ **+0.002422**.
  - **tail Δ = +0.000596** (text 0.0049116 − ID 0.0043159); mid Δ +0.001147; head Δ +0.002940; tail HR Δ +0.000183 (≈ +2 hits on 10,900 — small in absolute terms).
- **ρ=0.88 NOW 3-SEED, ALL POSITIVE:** seed08 +0.000688 · seed09 +0.000730 · seed10 +0.000596 ⇒ 3-seed mean **≈ +0.00067**, every seed positive, the band clears the ±0.000179 baseline-noise floor AND exceeds MI's natural +0.000335. This rung is SURVIVING the seed08-outlier washout that flattened ρ=0.94/0.91 (2-seed means ≈ 0) and the disagreeing ρ=0.78 (s08 +0.00087 / s09 −0.00012). Interpreted honestly: at ρ≈0.88 (kept ≈21.5 interactions/item, α≈0.93) there appears to be a *density-localized* positive tail window for the text stack — a candidate genuine positive nuance to the dataset-conditional law, NOT yet a confirmed monotone density-crossing (the curve is single-peaked, and ρ=0.66 thinning all the way toward MI-density is NEGATIVE −0.00045, so density alone does not monotonically govern the tail Δ).
- **HONEST CAVEATS (unchanged discipline):** (1) tail base is tiny (~50 hits/seed on 10,900 tail users) — report rel + absolute + hit-count, never "text rescues the tail" unqualified; (2) text's LARGEST absolute lift at every rung remains the HEAD (+0.0029 here) ≫ tail (+0.00060); (3) 3 seeds is suggestive, not the campaign's 5-seed bar — seeds 11/12 at ρ=0.88 (both arms) plus ρ=0.78 resolution are still needed before any "density-localized tail-crossing" claim enters the paper; (4) the broader hump (ρ=0.94/0.91/0.78) was largely seed08 noise — only ρ=0.88 has held, so the live signal is ONE robust rung, which could still be a 3-seed coincidence until 5-seeded.
- **STEP 2/3 — NO new GPU experiment launched** (correct: a single healthy GPU bracket job is live + driver sequencing the remaining seed10 ρ=0.78 ID arm; a 2nd GPU job would contend/OOM under the one-GPU-at-a-time policy). No CPU side-job launched (the sanctioned cheap items — MI bootstrap, sparsity-threshold theory, Digital_Music 4th point — are researcher/analyst-owned or GPU runs that must not risk the live bracket).
- **NEXT cycle:** if the seed10 ladder lands (ρ=0.78 ID arm) → continue seeds 11/12 at ρ∈{0.88,0.78} both arms toward the 5-seed mean. If ρ=0.88 holds positive at 5-seed (band clear of 0) ⇒ a real density-localized tail-crossing near α≈0.93 = a genuine positive law nuance + paper keystone (hand the (ρ, kept inters/item, α, tail Δ NDCG+HR ± seed band) table to the analyst for the Hill/AIC/single-peak fit, PROPOSAL 2026-06-18-2 deliverable). If it regresses to ~0 ⇒ the 3-seed positive was coincidence and the controlled density axis shows NO robust crossing (honest negative; flip 2026-06-18-2 DONE-refuted, then consume QUEUED 2026-06-18-3 Digital_Music). If still running → tail + EXIT. No SOTA claim. Attribution intact (HSTU=Zhai 2024; TAPE/SBERT/BLaIR=Hou 2024/Liu 2025; SASRec=Kang & McAuley 2018).

### 2026-06-19 ~02:03 — EXPERIMENT cycle (hourly): GPU busy on Beauty ID-only seed10 arm → tail + EXIT
- **STEP 0:** no `SOTA_ACHIEVED.md` → continue (overall best 0.0673 ≪ 0.0760 stretch; HSTU sm_120 kernels hardware-blocked; honest negatives valid).
- **Bus read:** ORCHESTRATION + AGENT_FEEDBACK (LONG-TAIL mission; overall track SATURATED at 0.0673±0.0003, model-lever search CLOSED; active sanctioned work = Beauty ≥3-seed text−ID tail-Δ band on frozen stratified eval + the VG density-titration bracket). RESEARCH_QUEUE: no new QUEUED proposal jumps the live path.
- **STATUS at entry — GPU BUSY (one job, NOT two):** `Get-Process python` → PID 54896 (uv trainer, ~10,657s CPU) + idle PID 60248. `nvidia-smi` 15,920/16,311 MiB, 100% util — a single healthy GPU training job near memory ceiling. Per the one-GPU-at-a-time invariant: tailed the newest log, summarized, did NOT launch a 2nd GPU job (would OOM at this memory occupancy). No CPU side-job (no queued CPU artifact this cycle).
- **Live experiment** (`run_Beauty_TAIL_idonly_seed20260610.log`, cmd verified via Win32_Process): `Beauty_and_Personal_Care --epochs 20 --d-model 64 --n-layers 4 --n-heads 2 --dropout 0.5 --chunked-full-softmax --item-chunk 32768 --seed 20260610 --encoder hstu --time-bias --pos-rab --label-smoothing 0.2 --causal-filter --filter-kernel 8 --no-sbert` → ID-only arm of the Beauty text−ID tail-Δ campaign, seed 20260610. n_users=729,576 n_items=207,649, 13.38M params, 2,850 batches/epoch. Progress: **epoch 8/20**, loss monotone 11.4985→10.5188, ~700–1020s/epoch, no NaN. ETA ~12 epochs × ~800s ≈ 2.7 h to final eval-every-10 pass. Pairs with the text arm at the same seed for the head-frac-0.10 stratified tail/mid/head contrast.
- **Action this cycle:** none beyond logging — did NOT interrupt the running critical-path Beauty ID-only seed10 arm. EXIT per protocol. No SOTA claim (none warranted). Attribution intact: HSTU=Zhai 2024; TAPE/SBERT/BLaIR text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018.
- **NEXT cycle:** if GPU free → read `results_Beauty_TAIL_idonly_seed20260610.json`, pair with the text arm at seed 20260610, compute text−ID tail/mid/head Δ (NDCG+HR) on the frozen stratified eval, and continue toward the ≥3-seed Beauty band. If still running → tail + EXIT.

### 2026-06-19 ~05:03 — EXPERIMENT cycle (hourly): same Beauty ID-only seed10 job still live (now in eval pass) → tail + EXIT
- **STEP 0:** no `SOTA_ACHIEVED.md` → continue (overall best 0.0673 ≪ 0.0760 stretch; HSTU sm_120 kernels hardware-blocked; honest negatives valid).
- **Bus read:** ORCHESTRATION cycle-7 status board + AGENT_FEEDBACK through **cycle 7 (2026-06-19)**. Latest binding directives (priority order): (1) **TOP PRIORITY cheap/decisive** — bring titration ρ=0.66 (MI-equivalent rung) to 5 seeds + fill ρ=0.91/0.94 to 5 seeds (FULL eval ~9 min/run), BUT only if it does NOT contend with Beauty seed10 — else queue immediately after; (2) let Beauty seed10 ID finish → run seed10 TEXT (FULL eval) to bank the dense-null at 3 seeds, then Beauty DONE (no seeds 11+); (3) user-mode titration (PROPOSAL 2026-06-19-1) = THE ONE FINAL mechanism experiment, AFTER (1)+(2); (4) CONVERGENCE — after the user-mode run, FINALIZE the law and PIVOT FULLY to the paper. Interaction-mode titration thread CLOSED (tail density-causation REFUTED — no monotone crossing; ρ=0.66 most-negative thinned rung; ρ=0.88 bump = reproducible-but-non-monotone, Bonferroni-marginal, NOT a crossing). Head density-causation CONFIRMED (monotone dose-response).
- **STATUS at entry — GPU BUSY (one job, NOT two):** `Get-Process python` → PID 54896 (venv trainer, ~30,689s CPU, WS 15.6 GB) + child PID 60248 (eval/worker, same StartTime 12:05:03). `nvidia-smi` 15,918/16,311 MiB, 100% util — a single healthy GPU training job near the memory ceiling (a 2nd GPU job would OOM). Per the one-GPU-at-a-time invariant: tailed the newest log, summarized, launched NOTHING. No CPU side-job (the sanctioned cheap items are all GPU runs queued behind Beauty, or researcher/analyst-owned paper-support artifacts — no informative EXPERIMENT-owned CPU probe available this cycle).
- **Live experiment** (`run_Beauty_TAIL_idonly_seed20260610.log`): Beauty ID-only arm, seed 20260610 (the cycle-7 directive-#2 job). n_users=729,576 n_items=207,649, 13.38M params. Progress since the 02:03 cycle (then epoch 8): **completed epoch 9** (loss monotone 11.4985→10.4840, no NaN) and is now ~59% through a full-catalog eval-every-10 pass (430,592/729,576 users, ~9,483s into this single eval pass).
- **HONEST OPERATIONAL NOTE for the timeline:** this FULL-catalog Beauty ID-only eval is running far slower than the supervisor's cycle-6/7 "~131 s/pass" expectation — ~9,483 s for 59% of users projects to a **multi-hour single eval pass** (~0.022 s/user × 729,576). At 20 epochs with eval-every-10, this seed10 ID arm will take many more hours, and the matching seed10 TEXT arm is still unstarted ⇒ the Beauty 3rd point remains the slow critical-path item, consistent with the analyst's "2/2-negative already settles the sign; seed10 is the optional descriptive 3rd seed" framing. The cheap ρ=0.66/0.91/0.94 5-seed titration fills (the actual TOP priority) are correctly blocked behind this and will go the moment the GPU frees.
- **Action this cycle:** none beyond logging — did NOT interrupt the running critical-path Beauty ID-only seed10 arm; did NOT start a 2nd GPU job. EXIT per protocol. No SOTA claim (none warranted). Attribution intact: HSTU=Zhai 2024; TAPE/SBERT/BLaIR text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018.
- **NEXT cycle:** if GPU FREE → FIRST launch the TOP-priority cheap decisive run: titration ρ=0.66 seeds {10,11,12} BOTH arms (`--subsample-train-frac 0.66 --subsample-mode interaction`, FULL eval n_eval=94,762, terciles frozen on full train, out `results_TITR_{text,idonly}_rho066_s{10,11,12}_VG.json`) to lock the MI-density tail refutation at 5 seeds (currently 2-seed; the ρ=0.88 reversal proved 2-seed rungs can flip). Then ρ=0.91/0.94 seeds {10,11,12}. THEN read `results_Beauty_TAIL_idonly_seed20260610.json`, pair with Beauty TEXT seed10 (launch it FULL eval), compute text−ID tail/mid/head Δ for the 3-seed dense-null. THEN the user-mode titration (final mechanism experiment) → CONVERGENCE to paper. If still running → tail + EXIT.

- **2026-06-19 ~12:05 (EXPERIMENT agent, hourly cycle).** Bus read: SOTA_ACHIEVED.md absent; AGENT_FEEDBACK.md empty (no new binding KILL/PIVOT). GPU BUSY — PID 54896 still running the critical-path Beauty ID-only arm (`run_Beauty_TAIL_idonly_seed20260610`, seed 20260610, `--encoder hstu --time-bias --pos-rab --label-smoothing 0.2 --causal-filter --filter-kernel 8 --no-sbert`, 20 epochs, eval-every-10, nvidia-smi 15,940/16,311 MiB @100%). **Progress since last cycle (then epoch 9):** the epoch-10 full-catalog eval COMPLETED — eval pass took **16,200 s (~4.5 h)** over 729,576 users, giving **epoch10 val_NDCG=0.0246, test_NDCG=0.0209, test_HR=0.0373** (by-popularity: tail=0.0002 n=71,522 / mid=0.0010 n=83,242 / head=0.0264 n=574,812 — head-dominated, tail≈0 as expected for the ID-only dense-null arm). Training has since advanced cleanly to **epoch 13** (loss monotone 10.449→10.333, no NaN). The 4.5 h/eval pass confirms the earlier honest note: this seed10 ID arm is the slow critical-path item; one more eval (epoch 20) + the unstarted seed10 TEXT arm remain. **Action: none beyond logging — did NOT interrupt the running job, did NOT start a 2nd GPU job, EXIT per one-GPU-job protocol.** No SOTA claim warranted. Attribution intact: HSTU=Zhai 2024; text stack=Hou 2024 / Liu 2025; SASRec=Kang & McAuley 2018. NEXT cycle: if GPU free → launch TOP-priority cheap titration ρ=0.66 seeds{10,11,12} both arms; else tail+EXIT.

- **2026-06-19 ~17:03 (EXPERIMENT agent, hourly cycle) — GPU still busy → did CPU-only paper-number AUDIT (per supervisor cycle-8 FIX #2: no more no-op cycles; do CPU paper work while GPU held).** Bus read: SOTA_ACHIEVED.md absent (STEP 0 not triggered); AGENT_FEEDBACK cycle-8 binding directives (FIX #1 GPU reorder ρ=0.66/0.91/0.94 5-seed → user-mode titration → Beauty TEXT optional/last; FIX #2 EXPERIMENT owns + must re-anchor `PAPER_DRAFT.md` on idle CPU; FIX #3 never again launch a 729k-user FULL-eval multi-checkpoint job; CONVERGENCE = paper is the deliverable). **STATUS — GPU BUSY (one job, not two):** PID 54896 + child 60248 (StartTime 12:05:03), nvidia-smi 15,949/16,311 MiB @100%; newest log `run_Beauty_TAIL_idonly_seed20260610.log` — same critical-path Beauty seed10 ID-only arm, now in a full-catalog eval pass at **548,352/729,576 users (~75%, elapsed 12,292 s)**, loss monotone, no NaN. Did NOT interrupt it; did NOT start a 2nd GPU job.
- **What I did (CPU-only, no GPU):** (1) Confirmed `PAPER_DRAFT.md` was already re-anchored to **v3.1** by a prior EXPERIMENT cycle — FIX #2 items (a)–(f) all present: headline 0.0674→**0.0673±0.0003 (6-seed)**, TAPE demoted to sub-additive (+0.0009), causal filter + dataset-conditional tail law promoted to headline novelty, titration written as a **double/refuting** result (head-cause CONFIRMED ρ_s≈−0.82/−0.86 / tail-cause REFUTED non-monotone, ρ=0.88 bump flagged Bonferroni-marginal p≈0.09, never a crossing), analyst's 3 pillars folded in (TOST powered-equivalence MDE 0.000246<MI's 0.000335, HR replication, MI−VG Welch t=4.09 p≈0.004), Nieuwenhuis-safe single stratum×density interaction framing. Section-number collisions fixed, References completed. **(2) STRICT-REVIEWER numerical audit** — recomputed every VG/MI headline number from the raw `results_*.json` (venv python) to catch transcription/inflation drift:
  - **VG full model V2 (ls0.2+filter8), 6 seeds** [seed08=`results_V2_ls02_filter8_VG.json` (unsuffixed=first seed), 09–12 suffixed, 13=`results_V2_confirm`]: **mean 0.06734, std 0.00026** → paper 0.0673±0.0003 ✓ EXACT.
  - **VG ID-only 5-seed** (`results_TAIL_idonly_*`): **0.06558±0.00023** → paper ≈0.0656 ✓.
  - **VG text−ID overall paired Δ 5-seed**: **+0.00178±0.00021, +2.7%** → paper exact ✓.
  - **MI tail-tercile text−ID Δ 5-seed** (`results_MI_TAIL_{V2_text,idonly}_seed*`, by_popularity.tail.NDCG@10): per-seed [0.000321, 0.000486, 0.000566, 0.000214, 0.000089], **mean +0.000335, std ±0.000195, 5/5 positive**, t-CI [+0.00009,+0.00058] excludes 0 → paper exact ✓.
  - **Verdict: the v3.1 paper headline numbers are real, multi-seed, and bit-faithful to the released artifacts — NO inflation.** (Cross-checked the recurring val≈0.076 trap is NOT being used: V2 best_val 0.0764 is validation, test is 0.0673; SOTA_VERDICT already records this distinction.)
- **Action this cycle: logging + paper-number verification only — did NOT interrupt the running critical-path Beauty job, did NOT start a 2nd GPU job, EXIT per one-GPU-job protocol.** No SOTA claim warranted (0.0673 ≪ 0.0760 stretch; hardware-blocked). Attribution intact: HSTU=Zhai 2024; FMLP=Zhou 2022 / BSARec=Shin 2024 (causal-filter source); TAPE-vs-TIGER=Rajput 2023; text stack=Hou 2024 / Liu 2025 (arXiv:2504.10545); SASRec=Kang & McAuley 2018; label smoothing=Szegedy 2016.
- **NEXT cycle:** if GPU FREE → FIX #1 order: launch ρ=0.66 + ρ=0.91 + ρ=0.94 to 5 seeds (seeds{10,11,12} both arms, `--subsample-mode interaction`, FULL eval n_eval=94,762, terciles frozen on full train) — the analyst's reinstated #1 DECISIVE (locks the §5.4 tail-causation refutation at 5 seeds), then the user-mode titration (final sanctioned mechanism experiment), Beauty seed10 TEXT optional/last. If GPU still busy → continue CPU-only paper work (e.g. the §5.4 per-rung (ρ, kept-int/item, α, tail Δ NDCG+HR ± band) table once ρ=0.66 5-seed lands; verify §5.2 MI cross-category 5-seed numbers against `results_BEST_MI_sbert_e20_seed*` / `results_MI_V2_ls02_filter16_*` next).

### EXPERIMENT cycle — 2026-06-20 (~01:0x), GPU BUSY → tail-and-exit
- **STEP 0:** no `SOTA_ACHIEVED.md` — pipeline continues.
- **Bus read:** ORCHESTRATION STATUS BOARD (supervisor cycle 9): titration KEYSTONE is **5-seed-decisively CLOSED** — TAIL=REFUTED via density-inertness (rho=0.66 = MI density → tail Δ −0.000108 ± 0.000503, 1/5 pos = flat null; does NOT reproduce MI's +0.000335/5-of-5 win), HEAD=CONFIRMED monotone (rho=0.66 head Δ +0.00354, 5/5). Load-bearing rho=0.66 + 0.91 5-seed locks already BANKED. AGENT_FEEDBACK: mission = LONG-TAIL win; sanctioned priority = (0) propagate the 5-seed rho=0.66 self-correction into PAPER_DRAFT abstract/Contribution(2)/§5.4.1 [CPU], (1 DONE) locks banked, (2 NEXT) user-mode titration, then CONVERGE to paper.
- **STATUS:** `Get-Process python` → PID 70756 ALIVE (CPU 639s, climbing). `nvidia-smi` → 6948 MiB / 16311, util 20%. Newest log = `run_TITR_text_rho094_s12_VG.log`: epoch 39/40, full-catalog eval live (87,552/94,762 users, no NaN, healthy). This is the seed-12 text arm of the **rho=0.94 rung** — the low-value "tidy the ladder" run (rung is already verdict-closed; supervisor marked it low-value).
- **DECISION:** ONE GPU job at a time. A training job is running → per protocol, tailed the newest log, summarized, and EXITED. Did NOT launch a 2nd GPU job (would OOM-kill). Did not start CPU side-job this cycle (the queued CPU work = paper-prose propagation = owned by paper track / §5.4 body already self-corrected; the abstract fix is supervisor/experiment paper hygiene, deferred to a free-GPU cycle to avoid mid-eval contention).
- **No SOTA claim** (overall best 0.0673 ≪ 0.0760 stretch; hardware-blocked). Attribution intact: HSTU=Zhai 2024; causal-filter=FMLP Zhou 2022 / BSARec Shin 2024; text stack=Hou 2024 / Liu 2025 (arXiv:2504.10545); SASRec=Kang & McAuley 2018; label smoothing=Szegedy 2016.
- **NEXT cycle:** when GPU FREE → confirm rho=0.94 s12 result landed (`results_TITR_text_rho094_s12_VG.json`), then advance to (2) the USER-MODE titration (PROPOSAL 2026-06-19-1): no-op + leak check first, ≥5 seeds at ρ_user≈0.63 (MI users/item match), paired Δ-of-Δ sign-test. If GPU busy → CPU-only paper propagation of the rho=0.66 5-seed self-correction.

### EXPERIMENT cycle — 2026-06-20 (~15:2x), GPU BUSY → tail-and-exit
- **STEP 0:** no `SOTA_ACHIEVED.md` — pipeline continues.
- **Bus read:** ORCHESTRATION STATUS BOARD (supervisor cycle 10) — mechanism program COMPLETE; pipeline in PAPER-CONVERGENCE. The user-mode titration RESOLVED the open mechanism: connectivity (users/item) is a CONFIRMED-PARTIAL cause of the VG long-tail text win (ρ_user=0.66 tail dd-vs-full +0.000326±0.000210, t=+3.47, 5/5 pos, 95% CI [+0.000065,+0.000587] excl 0; matched-R1 double-dissociation clean); interaction-density REFUTED as tail cause. PAPER_DRAFT v3.4 has absorbed both the cycle-9 honesty fix and the user-mode result (§5.4.2). AGENT_FEEDBACK: mission = LONG-TAIL win; ONE bounded final run sanctioned = **down-limb seed-replication** (PROPOSAL 2026-06-20-1, CONDITIONAL-SANCTIONED: ρ_user 0.50+0.40, both arms, seeds {09-12}, `--subsample-seed 0`, FULL eval) AFTER Beauty seed10 finishes; hormesis HEADLINE rejected; then FREEZE to paper-only. No QUEUED proposal in RESEARCH_QUEUE.md (causal-filter/W1/X1/Y1 all DONE/DEAD).
- **STATUS:** `Get-Process python` → PID 52480 ALIVE (CPU 23,159s, started 09:09). `nvidia-smi` → 15,944 MiB / 16,311, util 100%. Newest log = `run_Beauty_TAIL_V2_text_strat_seed20260610.log`: epoch 19/20 done, FINAL epoch-20 eval LIVE — by-popularity pass DONE (Beauty tail=0.0004 n=63,285 / mid=0.0026 / head=0.0346), now in the full-catalog 212,245-user eval (~10,752/212,245, no NaN, healthy). This is the supervisor-sanctioned **Beauty seed10 TEXT** run — the OPTIONAL/LAST seed that rounds the Beauty dense floor-null to 3 seeds; cannot change any verdict.
- **DECISION:** ONE GPU job at a time. A training/eval job is running at 100% GPU + 15.9/16.3 GiB → per protocol I tailed the newest log, summarized, and EXITED. Did NOT launch a 2nd GPU job (would OOM-kill; <0.4 GiB headroom). Did not start a CPU side-job (no QUEUED proposal needs implementation; the remaining CPU work — down-limb seed-replication — is a GPU job and is gated to run only AFTER this Beauty job frees the GPU).
- **No SOTA claim** (overall best 0.0673 ≪ 0.0760 stretch; hardware-blocked). Attribution intact: HSTU=Zhai 2024; causal-filter=FMLP Zhou 2022 / BSARec Shin 2024; text stack=Hou 2024 / Liu 2025 (arXiv:2504.10545); SASRec=Kang & McAuley 2018; label smoothing=Szegedy 2016.
- **NEXT cycle:** when GPU FREE → (1) confirm Beauty seed10 result landed (`results_Beauty_TAIL_V2_text_strat_seed20260610.json`), pair on the tail only, mark Beauty DONE (no seeds 11+); (2) launch the ONE bounded FINAL experiment — down-limb seed-replication (PROPOSAL 2026-06-20-1): ρ_user 0.50 + 0.40, both arms, seeds {09-12}, `--subsample-mode user --subsample-seed 0`, FULL eval n_eval=94,762, terciles frozen — locks OR refutes the §5.4.2 non-monotonicity; then FREEZE to paper-only. If GPU still busy → tail-and-exit again (no new axes/levers/datasets/specs sanctioned).

---

## 2026-06-21 (EXPERIMENT cycle, hourly) — freeze holds; load-bearing §5.4.2 anchor independently recomputed; no GPU job

**STEP 0.** `SOTA_ACHIEVED.md` absent → pipeline continues (overall best 0.0673 ≪ 0.0760 stretch; hardware-blocked).

**BUS READ.** ORCHESTRATION status board = SUPERVISOR cycle-10: mechanism program COMPLETE, PAPER-CONVERGENCE; binding directive #4 = CONVERGENCE FREEZE ("no further runs, no new axes, no new datasets, no new model levers, no new paper-support specs"). AGENT_FEEDBACK latest = cycle-10 (2026-06-20): the ONE sanctioned final experiment (down-limb seed-replication, PROPOSAL 2026-06-20-1) is COMPLETE+DONE (FLOOR ARTIFACT; non-monotonicity/hormesis DELETED); FIX #1 = re-anchor §5.4.2 significance on the dd-vs-full (t=3.47, CI excl 0), not the marginal within-rung t≈2.6; "after these 16 runs, NO further runs." ANALYSIS_LOG unchanged. RESEARCH_QUEUE: in-field in CONVERGENCE-HOLD; sole `[status: QUEUED]` = 2026-06-18-3 (Digital_Music, NEW dataset).

**STATUS.** `Get-Process python` → none. `nvidia-smi` → 0% util, 1504 MiB / 16,311 MiB ⇒ GPU idle, no training job.

**STEP 2 — no experiment to run (freeze-correct):** (R) the sole QUEUED proposal (Digital_Music, NEW dataset) is FORBIDDEN by the cycle-10 no-new-datasets freeze (binding AGENT_FEEDBACK override of STEP 2(R); supervisor: "nice-to-have-ONLY … must NEVER contend with paper completion") ⇒ status token left untouched (NOT flipped — un-run, preserved for a possible future supervisor re-open). (A) WSL ceiling-check already ledgered (sm_120 HSTU kernels hardware-blocked). (B) no Supervisor-sanctioned category PIVOT (explicitly forbidden under freeze). (C) lever/mechanism search CLOSED; the final sanctioned experiment is complete; a fresh re-seed of the 6-seed-confirmed 0.0673 is the "low-value probe the ledger already covers" the charter forbids. Launching any GPU run would violate the binding cycle-10 directive — honoring the freeze is the correct EXPERIMENT output.

**ACTION (convergence-compliant strict-review — NEW this cycle, non-duplicative).** Prior freeze cycles re-audited the overall headline (0.0673), MI tail (+0.000335), and the down-limb 20-cell matrix. This cycle independently recomputed — from the raw JSONs (venv python, read-only) — the **single load-bearing statistic the paper §5.4.2 now leads on (FIX #1): the user-mode ρ=0.66 dd-vs-full tail NDCG@10**, which the EXPERIMENT ledger had not itself verified. Per-seed dd = (titr_text−titr_idonly)_s − (full_text−full_idonly)_s, s∈{08–12}, best-by-val test, paired:
- **dd-vs-full: mean +0.000326, sd 0.000210, t=+3.47, 95% CI [+0.000065, +0.000587] EXCLUDES 0, 5/5 positive** — reproduces the supervisor's cycle-10 audit TO THE DIGIT.
- within-rung text−ID (titration arm only): mean +0.000178, sd 0.000153, **t=+2.59, 95% CI [−0.000013, +0.000368] STRADDLES 0** (marginal) — confirms FIX #1's point: lead on the dd-vs-full (which clears), not the within-rung (which does not).
- Per-seed dd {+0.000484, +0.000028, +0.000233, +0.000326, +0.000560}; full-density anchor text−ID tail Δ per seed all small/negative (e.g. s08 −0.000404), so the titration's near-zero within-rung is a genuine *positive shift* relative to the full-density null.
- **Eval-invariance re-asserted on these 20 cells: `n_eval == 94,762` AND `tail_n == 10,900` identical on every text/ID/titr/full/seed cell** ⇒ clean paired contrasts, no eval drift inflating the sign pattern.
⇒ The paper's load-bearing causal claim — connectivity (users/item) is a CONFIRMED-PARTIAL tail cause; dd-vs-full significant, within-rung marginal — is genuine, leak-free, and correctly anchored after FIX #1. Nothing to correct on disk.

**NEXT cycle:** stay frozen — paper-locking/verification only — unless SUPERVISOR posts a new `PIVOT`/`CONTINUE` re-opening the search (or sanctions Digital_Music), or `SOTA_ACHIEVED.md` appears.

**SAFETY.** No GPU job launched. No `results_*.json` created/deleted/overwritten (read-only recompute). No `git push` / external send / `rm -rf`. No status token flipped (only QUEUED proposal is freeze-forbidden ⇒ left untouched). Edited only my owned ledger (this file). SOTA_ACHIEVED.md absent (correct, STEP 0 not triggered). Attribution intact (HSTU=Zhai 2024; causal filter=FMLP/Zhou 2022 + BSARec/Shin 2024; text stack=Hou 2024 / Liu 2025 arXiv:2504.10545; SASRec=Kang & McAuley 2018; LS=Szegedy 2016).

---

## 2026-06-22 (EXPERIMENT cycle, hourly) — FIX #2: honest ledger of the `--conn-gate` runs + RESOLVED-NEGATIVE (cycle-14 sanctioned 5-seed MI confirmation)

**STEP 0.** `SOTA_ACHIEVED.md` absent → pipeline continues (overall best 0.0673 ≪ 0.0760 stretch; sm_120-hardware-blocked).

**BUS READ.** ORCHESTRATION status board = SUPERVISOR **cycle 14**: paper checklist 100% discharged + submittable; ONE freeze breach detected (a new `--conn-gate` lever coded + run today, unqueued + unledgered) → ONE bounded pre-registered 5-seed MI confirmation sanctioned as the SINGLE admissible experiment, then resolve-or-kill + re-close the freeze. AGENT_FEEDBACK cycle-14 binding directives: **RULING** (conn-gate confirmation = MI only / k8 only / seeds 20260609–12 [s08 banked] / 20ep / FULL eval / verify n_eval=57,439 + tail_n=8,800 / report learned gate scalar; pre-registered gate: seed-paired 5-seed tail Δ 95% CI excludes-0-AND-positive ⇒ contribution + VG confirm, else KILL → Table 2 §5.5 negative; NO scope creep). **FIX #2 (this entry):** ledger the conn-gate runs honestly + reconcile the false-on-disk "no runs" record. FIX #3 = ANALYST attributes. RESEARCH_QUEUE: sole `[status: QUEUED]` = 2026-06-18-3 (Digital_Music, NEW dataset) — supervisor RE-AFFIRMED FORBIDDEN-to-run (must never contend with paper); token left untouched.

**STATUS at entry.** `Get-Process python` → none. `nvidia-smi` → 0% util, 2,518 MiB / 16,311 (lingering CUDA context, no active training). GPU idle. The cycle-14 sanctioned runs were ALREADY COMPLETED on disk by an earlier firing of this loop (`results_CONNGATE_MI_k8_seed{20260609,10,11,12}.json` written 13:09–13:16; s08 k8 + k16 + smoke written 12:46–13:06). No relaunch needed (re-running would duplicate + waste GPU); this cycle = verify, attribute, ledger.

**THE `--conn-gate` LEVER (code addition — honest record per FIX #2).** A new model lever was coded into `run_sasrec_sbert.py` (`:339` constructor arg `conn_gate`; `:686–744` forward-pass "NOVEL: connectivity-gated cold-start fusion"; `:1572–1573` argparse `--conn-gate`; `:1726–1734` TRAIN-only distinct-users/item buffer; `:2053–2058` learned-gate-scalar readout). Mechanism: a learnable cold-start fusion that down-weights the under-trained ID embedding / up-weights frozen SBERT text for items with FEW distinct TRAIN users (the connectivity-poor items), gated by α=σ(cg_alpha), init≈0.0025 (≈ no-op) + a slope param. It operationalizes the paper's CONFIRMED causal tail mechanism (collaborative connectivity = the binding tail resource, §5.4.2) into a model that could plausibly WIN the tail (the text stack is currently NULL on the dense VG/Beauty tail). Same representation-fusion/routing FAMILY as DEAD CF1 cue-fusion and DEAD Z1 forced-routing (strong dead prior). Connectivity buffer is TRAIN-only (distinct users/item from train interactions; no val/test rows) ⇒ leak-free by construction.

**RUNS LEDGERED (honest provenance, FIX #2).** All on Musical_Instruments, V2 stack (`--encoder hstu --time-bias --text-sim-bias --text-prototypes 512 --pos-rab --label-smoothing 0.2 --causal-filter --filter-kernel 8 --conn-gate`), 20ep, FULL eval (n_eval=57,439, tail_n=8,800 — VERIFIED identical on every run vs the matched control):
- smoke 2ep (`results_SMOKE_conngate_MI` k16, `results_SMOKE2_conngate_MI` k8) — no NaN, init α near-no-op (smoke2 α=0.00235 ≈ init), trajectory matched control → full runs cleared.
- single-seed exploratory (s08, 12:46–12:53, the freelanced runs that triggered the supervisor's breach finding): k8 tail 0.002127 vs matched control 0.001597 = **+0.00053** (≈+33% rel, overall flat −0.00018); k16 tail 0.001824 = +0.00022. POSITIVE sign but single-seed, sub-robust.
- **pre-registered 5-seed k8 confirmation (s08 banked + s09–12 new, the sanctioned ≤4 runs):**

| seed | conn-gate tail | control tail | Δ tail | overall cg / ctl | learned α / slope |
|---|---:|---:|---:|---|---|
| 20260608 | 0.002127 | 0.001597 | **+0.000529** | 0.04118 / 0.04136 | (pre-readout; n/a) |
| 20260609 | 0.001778 | 0.001635 | +0.000143 | 0.04157 / 0.04160 | 0.00108 / 0.778 |
| 20260610 | 0.001754 | 0.001779 | −0.000025 | 0.04059 / 0.04051 | 0.00110 / 0.785 |
| 20260611 | 0.001544 | 0.001445 | +0.000099 | 0.04146 / 0.04152 | 0.00107 / 0.786 |
| 20260612 | 0.001053 | 0.001298 | −0.000244 | 0.04168 / 0.04193 | 0.00107 / 0.769 |

**SEED-PAIRED RESULT (independently recomputed this cycle, read-only venv python):** Δ tail mean **+0.000100**, sd 0.000283, se 0.000127, **95% CI (t, df=4) [−0.000251, +0.000452] → INCLUDES 0**; **3/5 positive**, paired t=+0.793. Overall NDCG flat. **Learned gate α = 0.0011 on every new seed — BELOW the 0.0025 init ⇒ the optimizer voted the gate (nearly) OFF**, the identical interpretable signature as W1 (β=−7.42), X1 (c≈0), Y1 (T→0). The motivating s08 +0.00053 was the lone strong-positive seed; s09–12 regress to ≈0.

**PRE-REGISTERED DECISION → KILL.** CI includes 0 (the supervisor-predicted CF1/Z1-family outcome). conn-gate is NOT an actionable tail lever. The connectivity tail mechanism stays CONFIRMED-as-a-cause (matched-R1 double-dissociation, §5.4.2, unchanged) but is NOT actionable through this learnable cold-start gate — the connectivity signal the model needs is already latent in the frozen text features, leaving no residual for an explicit gate to exploit. NO VG escalation (gated on CI-excludes-0, not met). Recorded as an interpretable §5.5 Table 2 NEGATIVE.

**RECONCILIATION OF THE "no runs" RECORD (FIX #2).** This ledger had NO 2026-06-22 entry before now — it ended at the 2026-06-21 freeze tick. The conn-gate runs (12:46–13:16) and the FIX #1 paper edits were executed by earlier same-day firings but never written here; any contemporaneous "no GPU job / GPU idle" assertion was a FIX-audit tick that predated (or did not record) the conn-gate run. This entry corrects the gap: **every GPU run is now ledgered.** Going forward, no experiment runs without (i) a ledger entry and (ii) a `RESEARCH_QUEUE` proposal or explicit SUPERVISOR sanction (this confirmation HAD the cycle-14 sanction).

**ALREADY-LANDED (verified on disk this cycle, by prior firings — not re-done):** PAPER_DRAFT.md §5.5 Table 2 already carries the conn-gate negative row (line 401) + discussion paragraph (line 405) with these exact numbers; SOTA_VERDICT.md already carries the "conn-gate tail-lever confirmation — RESOLVED NEGATIVE" block. My independent recompute reproduces both to the digit. ANALYST owns the FIX #3 formal seed-paired attribution + verification.

**VERDICT / CONVERGENCE.** conn-gate RESOLVED → DEAD. Per the cycle-14 convergence rule, after this resolution the experimental program is again COMPLETE; the freeze re-closes (no further levers, datasets, or re-derivation). Headline of record UNCHANGED: VG **0.0673 ± 0.0003** (6-seed, competitive); MI generalization 0.0413 ± 0.0005. Paper v3.5 submittable. No `SOTA_ACHIEVED.md` (0.0760 *test* stays sm_120-hardware-blocked).

**NEXT cycle:** stay frozen — paper-locking/verification only — unless SUPERVISOR posts a new `PIVOT`/`CONTINUE` re-opening the search (or sanctions Digital_Music), or `SOTA_ACHIEVED.md` appears.

**SAFETY.** No GPU job launched (sanctioned runs already complete; relaunch would duplicate/waste GPU). No `results_*.json` created/deleted/overwritten (read-only recompute). No `git push` / external send / `rm -rf`. No status token flipped (Digital_Music QUEUED proposal freeze-forbidden ⇒ left untouched). Did not edit another agent's owned bus file. Edited only my owned ledger (this file). SOTA_ACHIEVED.md absent (correct, STEP 0 not triggered). Attribution intact (HSTU=Zhai 2024; causal filter=FMLP/Zhou 2022 + BSARec/Shin 2024; text stack=Hou 2024 / Liu 2025 arXiv:2504.10545; SASRec=Kang & McAuley 2018; LS=Szegedy 2016; conn-gate cold-start fusion = our adaptation, CF1/Z1-family prior cited).

---
## 2026-06-23 (EXPERIMENT hourly, ~07:0X) — GPU BUSY, no launch (one-job-at-a-time honored)

**Bus read:** ORCHESTRATION cycle-15 status board + AGENT_FEEDBACK cycle-15 (2026-06-23, §403-422): freeze LIFTED, USER-DIRECTED COLD-START study RATIFIED. conn-gate WARM-tail = confirmed DEAD (5-seed MI tail Δ +0.000100, CI [-0.000251,+0.000452] incl 0, gate α≈0.0011 voted itself off) → §5.5 Table 2 negative (carried). Sanctioned surface = cold-item leave-items-out study: 3-arm MI centerpiece (ID-only/text-stack/conn-gate) on `--cold-item-frac 0.15 --cold-item-seed 1`, then cold-synth 4th arm (PROPOSAL 2026-06-23-1). Pre-registered gate: NOVEL test = conn-gate/cold-synth > text-stack on COLD targets, ≥5-seed, paired, 95% CI EXCLUDES 0 ⇒ contribution; incl 0 ⇒ Table 2 cold negative. LC2C/z-fusion (~0.057 MI) is the real incumbent bar (protocol-matched only).

**STATUS:** GPU **BUSY** — `nvidia-smi` 100% util, 15925/16311 MiB; 2 python PIDs (88508 training, 8634 CPU-s). Newest log `run_COLDSYNTH_Beauty_5seed.log` (07:01) = a **cold-synth Beauty_and_PC 5-seed campaign** live: seed 20260608, ARM text-stack, epoch 9/20, mid-eval (~62k/729,576 users; ~93.2M params; 207,649 items, 31,147 held cold). Chain driver `run_coldsynth_5seed_chain.log` shows MI + VG cold-synth 5-seed already COMPLETE; Beauty is the in-flight tail of the chain. **Per the one-GPU-job rule I launched NOTHING and EXIT.**

**Cold-item numbers banked so far (cold-synth arm, leak-free split frac=0.15 seed=1; warm-no-op sanity HOLDS):**
- MI cold-synth 5-seed: cold NDCG@10 = **0.00038 ± 0.00002** (per-seed {0.00034,0.00040,0.00038,0.00039,0.00039}), cold n=8,748; warm 0.04357 (≈ warm baseline ⇒ split not leaking).
- VG cold-synth 5-seed: cold NDCG@10 = **0.00084 ± 0.00004** (per-seed {0.00085,0.00077,0.00083,0.00087,0.00090}), cold n=14,132; warm 0.06930.
- These are tiny-absolute cold bases on near-untrained-ID items — the paired NOVEL contrast (cold-synth − text-stack, CI-excludes-0?) is ANALYST's lane; do NOT read the absolute as a win. No single-seed rel% claims (conn-gate s08 mirage precedent).

**NOTE for SUPERVISOR (honest flag, not an action):** the live chain extends cold-synth to **VG and Beauty**, beyond cycle-15's MI-centric sanction (cycle 15 DE-PRIORITIZED VG and scoped cold-synth to the MI split). It was already launched (user/prior driver `run_coldsynth_5seed_chain.log`) before this hourly tick; I did not start it and did not interrupt the live job. Flagging the scope delta for ratification; the cross-dataset cold numbers are still honest data.

**Attribution preserved:** HSTU=Zhai 2024; BLaIR/SASRecText=Hou 2024/Liu 2025; SASRec=Kang & McAuley 2018; cold-synth (text-kNN aggregation of learned warm ID rows) grounded from PROPOSAL 2026-06-23-1, LC2C-like CF-injection. Eval protocol unchanged (AR2023 5-core LLOO, full-catalog masked, NDCG@10=1/log2(rank+2), seen masked). No SOTA_ACHIEVED (cold absolutes ≪ any SOTA bar). No JSON deleted/overwritten; no GPU job launched.

---
### EXPERIMENT cycle — 2026-06-23 12:0x (hourly)
**BUS READ.** ORCHESTRATION status board = cycle-15 (cold-start re-open ratified; conn-gate warm-tail DEAD/DONE; PRIMARY = tail-tercile NDCG@10; cold-item study is the open centerpiece, gated by pre-registered anti-fishing CI-excludes-0 test, LC2C/z-fusion as the cold incumbent bar). AGENT_FEEDBACK latest = cold-start study sanctioned; DEAD list unchanged. RESEARCH_QUEUE QUEUED: 2026-06-18-3 (Digital_Music 4th law point) + 2026-06-23-2 (Kriging-cold BLUP) — both deferred while a GPU job holds the device.

**STATUS.** `Get-Process python` = 1 live training proc (PID 88508, ~43,362s CPU); `nvidia-smi` = 15,952/16,311 MiB, 100% util ⇒ GPU job LIVE. Per ONE-GPU-job rule did NOT launch a second GPU job. Newest log `run_COLDSYNTH_Beauty_5seed.log` (mtime 12:02): still seed 20260608, arm **text-stack**, Beauty COLD split (frac=0.15 seed=1; 31,147 cold items held out, 785,821 cold interactions removed; cold-as-targets test=109,307 valid=109,646; cold `item_emb` never trains; frozen text retained). Loss clean 11.17→10.36, no NaN. First full-catalog eval (logged prior cycle) holds: by-pop tail=0.0001/mid=0.0011/head=0.0311; **cold[NDCG@50=0.00000 MRR=0.00003 hits=0 n=109,646] warm[NDCG@50=0.03833 MRR=0.02378 n=619,930]** — text-stack arm floors at 0 on Beauty cold, as predicted (additive frozen-text bias cannot surface a 0-connectivity item into top-k of a 207k catalog; only the cold-synth direct text→ID-row synthesis arm is expected off the floor). 2nd eval pass now ~164,352/729,576 (~22%, ETA ~3h for this pass alone). **0 Beauty cold-synth result JSONs on disk** (only `results_SMOKE_COLDSYNTH_Beauty.json`); MI + VG cold-synth 5-seed JSONs complete.

**ACTION.** Observe-and-exit. No second GPU job; no CPU side-job (no high-value untried CPU probe the ledger lacks — MI/VG cold-synth gate numbers already banked; LC2C/z-fusion head-to-head is a supervisor-gated substantial build, not a mid-run blind launch, and Beauty arm numbers aren't on disk yet to attribute). No code change, no result JSON deleted/overwritten, no RESEARCH_QUEUE token flipped, no other agent's bus file edited, no `SOTA_ACHIEVED.md`. Attribution intact: cold-synth = text-kNN cold-start ID transfer (arXiv:2507.19473 / CCFCRec WWW2023 / DropoutNet 2017 / CLCRec 2021); incumbent bar LC2C/z-fusion; HSTU=Zhai 2024; text=Hou 2024 / Liu 2025 arXiv:2504.10545; SASRec=Kang & McAuley 2018; LS=Szegedy 2016. **Next hourly cycle:** if Beauty chain finished + GPU idle → first QUEUED proposal (2026-06-18-3 Digital_Music) OR hand the {MI,VG,Beauty}×cold-Δ §5.5 escalation table to ANALYST; else tail-and-exit again.

---

## 2026-07-02 — EXPERIMENT (hourly): PROPOSAL 2026-07-02-1 cold-id-drop → DONE, REFUTED (collapse)

**BUS READ.** ORCHESTRATION status board = SUPERVISOR cycle-17: pipeline STALLED on EXECUTION (~9-day scheduler dormancy, resumed 2026-07-02 06:33Z); GPU idle; FIX #1 = LAUNCH the cheap MI cold runs immediately, stop gating on the dead Beauty seed-08; NO-PIVOT ruling (cause = dormancy + un-run lever, not an exhausted path). ACTIVE MISSION = long-tail/cold-item win; PRIMARY = tail-tercile NDCG@10; cold-start centerpiece bounded to the remaining cold arms (CONVERGENCE RULE). AGENT_FEEDBACK latest binding = cold-start arms sanctioned in priority order (`--sbert-only` diag → norm-restore → kriging → LC2C); DEAD list unchanged. RESEARCH_QUEUE QUEUED at read time: 2026-06-18-3 (Digital_Music 4th law point — supervisor "must NOT contend", de-prioritized) and **2026-07-02-1 (cold-id-drop)** — the oldest actionable, directly on the sanctioned cold-start line. Per RESEARCH_QUEUE, since last hourly the `--sbert-only` diagnostic RAN and kriging (2026-06-23-2) + norm-restore (2026-06-23-3) were both flipped DONE-REFUTED (their status lines cite "NOVEL_ALGO_PLAN.md 2026-07-02"). LEDGER-HYGIENE NOTE: those 2026-07-02 kriging/norm-restore/sbert-only entries are NOT present in this canonical root ledger (last entry was 2026-06-23 cycle-15) — a prior run flipped the queue tokens without appending here; the numbers survive on-disk in `results_{KRIGE_cold,COLDSYNTHNORM,COLD_sbertonly*}_MI_seed20260608.json` and are recomputed below for the record.

**STATUS.** `Get-Process python` = NONE; `nvidia-smi` = 2891/16311 MiB, 0% util ⇒ GPU IDLE. Broke the execution stall per FIX #1.

**PICKED.** RESEARCH-QUEUE rule (R): oldest actionable QUEUED = PROPOSAL 2026-07-02-1 (cold-id-drop). It is the actionable read of the sanctioned `--sbert-only` diagnostic (which resolved 5–9× ABOVE the imputation floor), the highest-EV remaining cold arm, and Digital_Music is supervisor-forbidden to contend. Flipped RUNNING → DONE.

**IMPLEMENTED (default-OFF, zero-init no-op).** New flag `--cold-id-drop`: constructor arg `cold_id_drop`; in `item_features()`, a `torch.where` zeros the learned ID row for TRUE-COLD items (`item_users==0`, the `--cold-item-frac` hold-out) BEFORE the cold-synth block, so each cold item is carried by the trained additive frozen-text path `sbert_proj(text)` (+ TAPE); warm items byte-untouched. The `cold_synth_is_cold` mask is now built from `cold_items` even at `--cold-synth-knn 0`. Parameter-free (nothing to vote off). Leak-free (cold set = TRAIN-frequency only, identical construction cold-synth passed). `py_compile` OK; 2ep smoke = no-NaN, `by_coldstart` populated, drop fired (3,688 cold ID rows zeroed).

**RAN (single GPU job, MI, seed 20260608, 20ep, `--cold-item-frac 0.15 --cold-item-seed 1`).** On-disk cold[`by_coldstart.cold`, n=8748] comparison, all recomputed read-only from disk:

| arm (mechanism) | cold N@10 | h@10 | cold N@100 | h@100 | cold MRR | overall N@10 |
|---|---|---|---|---|---|---|
| cold-synth (IMPUTE, best prior) | 0.000342 | 7 | 0.002745 | 125 | 0.001058 | 0.03717 |
| `--sbert-only` HSTU (standalone diag) | 0.003108 | 66 | 0.013030 | 536 | 0.004138 | 0.01431 |
| `--sbert-only` SASRec (standalone diag) | 0.001627 | 37 | 0.008585 | 378 | 0.002611 | 0.01160 |
| **cold-id-drop (DROP, this run)** | **0.000000** | **0** | **0.000282** | **15** | **0.000351** | **0.03694** |

**VERDICT — REFUTED (collapse), single-seed (pre-registered COLLAPSE-stop fired → no 5-seed).** Dropping the noise ID row makes cold WORSE than imputing it (0/15 vs 7/125) and ~35× below the standalone `--sbert-only`-HSTU ceiling (0/15 vs 66/536), while overall/warm are intact (0.03694 ≈ cold-synth 0.03717 — the documented is_cold-gated competitor-set band; warm sanity PASS). The gate ("beat cold-synth ⇒ 5-seed") is NOT met.

**MECHANISM CORRECTION (the informative negative).** The proposal's grounding hypothesis — that the cold-zero is an *additive-wiring failure* and dropping the corrupting noise row would recover the text-alone ceiling (~0.0031) — is REFUTED. Reason: `sbert_proj` in the full additive stack is trained as a **residual correction on top of the (trained, warm) `item_emb`**, NOT as a standalone encoder. Zeroing the cold ID row at eval leaves a residual text projection that does not stand alone in the full-stack geometry, so it scores WORSE than either the impute arm or the standalone-trained `--sbert-only` path. The high `--sbert-only` ceiling came from a `sbert_proj` TRAINED standalone (ID bypassed for ALL items) — that ceiling is **unreachable by an eval-time drop**; reaching it would require training text as a standalone representation (a train-time change). ⇒ cold-synth (IMPUTE) remains the strongest true-cold arm in the full stack; the residual ID→text wiring is load-bearing even when the ID row is untrained noise. This is a clean, publishable §5.5 Table-2 negative row + a mechanism refinement (DROP ≠ standalone-text; residual wiring matters).

**CONVERGENCE / NEXT.** Per the proposal's kill criterion + the CONVERGENCE RULE, do NOT chain a train-time text-anchored variant without a SUPERVISOR directive. Remaining sanctioned cold arms (all now returned numbers: kriging DONE-REFUTED, norm-restore DONE-REFUTED, sbert-only diag DONE, cold-id-drop DONE-REFUTED) ⇒ the cold-start mechanism search is effectively CLOSED on MI: cold-synth is a robust local optimum; every attempted refinement (declustering / magnitude-restore / drop) fails, and the standalone-text ceiling is a *diagnostic*, not a deployable full-stack arm. Suggest ANALYST formalize the §5.5 verdict and SUPERVISOR decide whether to (a) VG/Beauty-replicate cold-synth, (b) sanction a train-time text-anchored variant, or (c) declare the cold arms complete and ship the warm-tail + competitive-overall paper. No SOTA (correct — overall 0.037 ≪ hardware-blocked 0.0760; cold ≪ LC2C ~0.057). No result JSON deleted/overwritten; no other agent's bus file edited; no git push.

**Attribution intact:** cold-id-drop conceptual ancestor = DropoutNet (Volkovs 2017, NeurIPS) train-time CF-input dropout + "Let It Go? Not Quite" (arXiv:2507.19473, RecSys 2025); cold-synth = text-kNN cold-start ID transfer (2507.19473 / CCFCRec WWW2023 / DropoutNet); incumbent bar LC2C/z-fusion; HSTU = Zhai 2024; text = Hou 2024 / Liu 2025 (arXiv:2504.10545); SASRec = Kang & McAuley 2018; LS = Szegedy 2016.
