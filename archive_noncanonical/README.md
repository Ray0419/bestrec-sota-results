# Archived NON-CANONICAL publication tracks (full-method audit F1, 2026-07-10)

These artifacts belong to SUPERSEDED publication tracks and are NOT part of the
canonical submission (see /CANONICAL_SUBMISSION.md):

- `_paper_gen/` + `BEST_Rec_v4_Full_Paper.pdf` — the old BEST-Rec v4 / LC2C / EASE
  paper and its generator (its own manifest recorded `sota_claim_allowed: false`).
- `lc2c_era_tables.json`, `lc2c_era_results_manifest.json` — generated artifacts of
  that era (moved from `_bestrec_run/`).
- The LC2C Retrieval-LTR lab paper remains in `_bestrec_sota_lab/paper_draft/` as a
  separate NON-CANONICAL workstream (marked there); it is a possible future paper
  (audit Path B) but is not this submission.

Nothing here feeds the canonical paper's artifact graph.
