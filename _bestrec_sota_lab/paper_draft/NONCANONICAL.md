This LC2C Retrieval-LTR draft is NOT the canonical submission (see /CANONICAL_SUBMISSION.md).
It is a separate candidate track (full-method audit Path B). If revived, it requires the
audit's fairness reframe: DropoutNet-assisted learned fusion claim + no-DropoutNet ablation.
